classe random contient
    methode constructeur () fait
        !init_random();
    fin
    
    methode rand (max) fait
        "Renvoie un nombre aléatoire r : r ∈ ⟦0, max⟧";
        renvoie !w_random(0, max);
    fin

    methode random(min, max) fait
        "Renvoie un nombre aléatoire r : r ∈ ⟦min, max⟧";
        renvoie !w_random(min, max);
    fin
fin

