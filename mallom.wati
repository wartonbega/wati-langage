inclue "entiers.wati";
inclue "erreur.wati";
inclue "bool.wati";

externe <*rien> _malloc(<ent>); // Utile pour allouer de la mémoire grossièrement à 
externe <rien> _free(<*rien>);

fonction <*rien> mallom(<ent> taille) fait 
    renvoie !_malloc(taille);
fin

fonction <rien> libere(<*rien> taille) fait 
    !_free(taille);
fin
