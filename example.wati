fonction test fait
    pour i allant de 1 a 10 fait
        affiche i;
        affiche i + 1;
        i = 2;
    fin
fin

fonction func (x) fait
    renvoie x ** 2;
fin

si a == 5 alors
    !func (a);
    a = !func (a);
fin

fonction principales () fait
fin

classe string contient
    self.content = '';
    
    methode constructeur (self, char) fait
        self.content = char;
        renvoie self;
    fin

    methode en_string (self) fait
        renvoie self.content;
    fin
fin